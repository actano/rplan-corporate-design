import { Meta, Story } from '@storybook/addon-docs/blocks'
import { Grid } from '@material-ui/core'
import { DueDateStatus, DUE_DATE_STATUS } from '../../../../src/components'

import { decorators } from '../../decorators'

<Meta
  title="Components/Labels/Time Status"
  decorators={decorators}
/>

export const TemplateTimeStatus = (args) => (
  <Grid container direction="column" spacing={1}>
    <Grid item xs={2}>
      <DueDateStatus
        state={DUE_DATE_STATUS.ON_TIME}
        delta={0}
        {...args}
      />
    </Grid>
    <Grid item xs={2}>
      <DueDateStatus
        state={DUE_DATE_STATUS.AT_RISK}
        delta={2}
        {...args}
      />
    </Grid>
    <Grid item xs={2}>
      <DueDateStatus
        state={DUE_DATE_STATUS.OVERDUE}
        delta={4}
        {...args}
      />
    </Grid>
    <Grid item xs={2}>
      <DueDateStatus
        state={DUE_DATE_STATUS.INAPPLICABLE}
        delta={0}
        {...args}
      />
    </Grid>
  </Grid>
)


# Time Status

<Story
  name="Default"
  args={{}}
>
  {TemplateTimeStatus.bind({})}
</Story>

