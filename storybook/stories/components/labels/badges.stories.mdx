import { Meta, Story } from '@storybook/addon-docs/blocks'
import { Grid } from '@material-ui/core'
import { Badge } from '../../../../src'

import { decorators } from '../../decorators'

<Meta
  title="Components/Labels/Badges"
  decorators={decorators}
/>

export const TemplateBadges = (args) => (
  <Grid container direction="column" spacing={1}>
    <Grid item>
      <Badge text="responsible" {...args} />
    </Grid>
    <Grid item>
      <Badge text="custom badge text" {...args} />
    </Grid>
  </Grid>
)

# Badges

<Story
  name="Default"
  args={{}}
>
  {TemplateBadges.bind({})}
</Story>
