import { Meta, Story } from '@storybook/addon-docs/blocks'

import { decorators } from '../../decorators'
import {
  DotMenu, DotMenuItem,
} from '../../../../src'

<Meta
  title="Components/Buttons/Dot Menu"
  decorators={decorators}
/>

export const Template = (args = {}) => (
  <DotMenu {...args}>
    <DotMenuItem onClick={() => {}}>Item one</DotMenuItem>
    <DotMenuItem onClick={() => {}}>Item two</DotMenuItem>
  </DotMenu>
)

# Size default
<Story
  name="Size default"
  args={{
    size: 'default',
  }}
>
  {Template.bind({})}
</Story>

# Size small
<Story
  name="Size Small"
  args={{
    size: 'small',
  }}
>
  {Template.bind({})}
</Story>
