import { Grid } from '@material-ui/core'
import { Meta, Story } from '@storybook/addon-docs/blocks'

import { decorators } from '../../decorators'
import { CustomControlledInput } from './controlled-input'

<Meta
  title="Components/Input/Controlled Input"
  decorators={decorators}
/>

**TODO: Extract corporate design input that encapsulates the basic styling**

export const Template = (args) => (
  <Grid container direction="column" spacing={1}>
    <Grid item>
      <CustomControlledInput
        fullWidth
        originalValue="Some text"
        style={{
          border: '1px solid black',
        }}
        {...args}
      />
    </Grid>
  </Grid>
)

# Default
<Story
  name="Default"
  args={{}}
>
  {Template.bind({})}
</Story>

# Disabled
<Story
  name="Disabled"
  args={{ disabled: true }}
>
  {Template.bind({})}
</Story>

