import { Grid } from '@material-ui/core'
import { Meta, Story } from '@storybook/addon-docs/blocks'

import { InputWithLabel } from '../../../../src/inputs/input-with-label'
import { decorators } from '../../decorators'
import { CustomControlledInput } from './controlled-input'

<Meta
  title="Components/Input/Input With Label"
  decorators={decorators}
/>

export const Template = (args) => (
  <Grid container direction="column" spacing={1}>
    <Grid item>
      <InputWithLabel label="Full Name">
        <CustomControlledInput
          fullWidth
          originalValue="John Doe"
          style={{
            border: '1px solid black',
          }}
          {...args}
        />
      </InputWithLabel>
    </Grid>
  </Grid>
)

# Label attachment for inputs

This wrapper provides a label to any given input.

<Story
  name="Default"
  args={{}}
>
  {Template.bind({})}
</Story>
