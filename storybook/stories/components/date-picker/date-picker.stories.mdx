import { Meta, Story } from '@storybook/addon-docs/blocks'
import { actions } from '@storybook/addon-actions'

import { ChangeOnAcceptDatePicker } from '../../../../src/date-pickers'

import { decorators } from '../../decorators'

<Meta
  title="Components/Date Pickers/Date Picker"
  decorators={decorators}
/>

export const TemplateDatePicker = (args) => {
  const onSelectDate = actions('onSelectDate')
  return (
    <ChangeOnAcceptDatePicker {...args} {...onSelectDate} />
  )
}

# Date Picker
<Story
  name="Default"
  args={{}}
>
  {TemplateDatePicker.bind({})}
</Story>

# With min and max date
<Story
  name="With min and max date"
  args={{
    minDate: '2021-01-05',
    maxDate: '2021-01-15',
    value: '2021-01-08',
  }}
>
  {TemplateDatePicker.bind({})}
</Story>

# With disableWeekends
<Story
  name="With disabled weekends"
  args={{
    value: '2021-01-08',
    disableWeekends: true,
  }}
>
  {TemplateDatePicker.bind({})}
</Story>
