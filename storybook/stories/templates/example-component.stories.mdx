import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks'
import { Grid } from '@material-ui/core'

import { decorators } from '../decorators'

import { ExampleComponent } from './example-component'

<Meta
  title="_Templates/ExampleComponentMDX"
  component={ExampleComponent}
  decorators={decorators}
/>

export const Template = args => (
  <Grid container direction="column" spacing={1}>
    <Grid item>
      <ExampleComponent {...args} />
    </Grid>
    <Grid item>
      <ExampleComponent {...args} />
    </Grid>
  </Grid>
)

# Some documentation headline
Some documentation text

<Story
  name="ExampleStory1"
  args={{}}
>
  {Template.bind({})}
</Story>

# Some other headline
Some other text
<Story
  name="ExampleStory2"
  args={{ someValue: 'foo' }}
>
  {Template.bind({})}
</Story>

# Automatically list all typed properties of the comoponent
<ArgsTable of={ExampleComponent} />
