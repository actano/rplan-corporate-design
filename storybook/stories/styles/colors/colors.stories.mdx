import { Meta, Story } from '@storybook/addon-docs/blocks'
import { Grid } from '@material-ui/core'

import { ColorItem, theme } from './color-item'
import { decorators } from '../../decorators'
import { colorCategories, uncategorizedColors } from './color-categories'

<Meta
  title="Styles/Colors"
  decorators={decorators}
/>

# Colors

## Neutrals
<Story name="Neutrals">
  <Grid container spacing={1} direction="row">
    { colorCategories.neutrals.colors.map(ColorItem) }
  </Grid>
</Story>

## Signature palette
<Story name="Signature palette">
  <Grid container spacing={1} direction="row">
    { colorCategories.signature.colors.map(ColorItem) }
  </Grid>
</Story>

## Support
<Story name="Support">
  <Grid container spacing={1} direction="row">
    { colorCategories.support.colors.map(ColorItem) }
  </Grid>
</Story>

## Signal
<Story name="Signal">
  <Grid container spacing={1} direction="row">
    { colorCategories.signal.colors.map(ColorItem) }
  </Grid>
</Story>

## Uncategorized
<Story name="Uncategorized">
  <Grid container spacing={1} direction="row">
    { uncategorizedColors.map(ColorItem) }
  </Grid>
</Story>
